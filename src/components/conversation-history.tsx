import React from 'react';
import { IConversationItem } from './types';

interface IConversationHistoryProps{
    chatHistory : IConversationItem[];
}
const ConversationHistory = (props: IConversationHistoryProps) => {
    return (
        <div>
            
        </div>
    );
};

export default ConversationHistory;